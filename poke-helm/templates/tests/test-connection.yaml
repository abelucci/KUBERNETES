apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poke-helm.fullname" . }}-test-connection"
  labels:
    {{- include "poke-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poke-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
